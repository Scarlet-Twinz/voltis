# VOLTIS local container stack
DB_USER=voltis
DB_PASSWORD=voltis_dev_password
DB_NAME=voltis
REDIS_PASSWORD=

# Use strong random values outside local development.
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=15m
WORKER_SECRET=replace-with-a-long-random-worker-secret
